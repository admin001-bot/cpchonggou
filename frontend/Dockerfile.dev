# 开发环境 Dockerfile - 支持热重载
FROM node:20-alpine

WORKDIR /app

# 安装依赖
COPY package*.json ./
RUN npm install

# 复制源码
COPY . .

EXPOSE 3000

# 使用Vite开发服务器
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
