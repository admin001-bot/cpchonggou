# 服务器配置
server:
  port: 8080
  mode: debug  # debug, release, test

# 数据库配置
database:
  host: 127.0.0.1
  port: 3306
  database: xin1986ly
  username: root
  password: Aa123456
  charset: utf8mb4
  prefix: ssc_

# JWT配置
jwt:
  secret: lottery-system-jwt-secret-key-2024
  expire: 24h
  issuer: lottery-system
